package com.yunho.king.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// 다크 네이비 + 블루 계열 브랜드 컬러
val KingDarkBackground = Color(0xFF050A12)   // 전체 배경
val KingDarkSurface = Color(0xFF111926)      // 카드/시트 배경

// 메인 포인트 색 (브랜드 블루)
val KingPrimary = Color(0xFF2F7DFF)
val KingOnPrimary = Color(0xFFFFFFFF)

// 텍스트 / 아이콘
val KingOnBackground = Color(0xFFE5EEF8)
val KingOnSurface = Color(0xFFC5D0E0)

// 경계선/디바이더
val KingOutline = Color(0xFF304366)
